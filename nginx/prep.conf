server {
    listen 80;
    server_name _;

    location / {
        include proxy_params;
        proxy_pass http://unix:/home/ubuntu/Prep_Mining/myflaskapp.sock;
    }

    location /static/ {
        alias /home/ubuntu/Prep_Mining/static/;
    }
}
